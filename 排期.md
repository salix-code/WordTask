# 📅 单词记忆 App (敏捷开发版) 工程任务排期表

本项目分为四个主要的里程碑（Milestones）。采用敏捷开发策略：先通过 Mock 数据跑通前端 UI 与核心交互，再逐步接入后端骨架、数据库与核心记忆算法。

## 🚩 Milestone 1: 前端 UI 搭建与 Mock 数据排版 (MVP 视觉版)
**目标**：不依赖任何后端，纯前端搭建完整的移动端卡片交互体验，使用固定 JSON 数组作为数据源。

- [ ] **Task 1.1: 前端 (`web`) 初始化**
    - 使用 Vite 创建 `web` 目录（Vue 3 + TypeScript 模板）。
    - 配置 Tailwind CSS 和 DaisyUI。
- [ ] **Task 1.2: 静态 UI 组件开发**
    - 开发顶部导航（进度展示：例如 10/50）。
    - 开发居中的**单词卡片组件**（Card UI，带阴影和圆角）。
    - 开发底部的操作栏（红、黄、绿三个大按钮，高度 >= 44px 适配移动端）。
- [ ] **Task 1.3: 交互与 Mock 数据流**
    - 在前端硬编码一个 JSON 数组作为 Mock 词库（包含几个带音标、释义的固定单词）。
    - 实现**点击翻转**卡片显示释义的交互逻辑。
    - 集成 **Web Speech API**，实现点击发音。
    - 实现核心闭环流：点击底部的红黄绿按钮后，卡片切换到下一个单词。直到数组遍历完毕，显示“今日已完成”。

## 🚩 Milestone 2: 后端基础与前后端通信打通
**目标**：搭建 Go 后端骨架，将前端的 Mock 数据移到后端内存中，跑通前后端 API 交互。

- [ ] **Task 2.1: 后端 (`server`) 初始化**
    - 创建 `server` 目录，初始化 Go Modules。
    - 搭建基于 Gin 的基础 HTTP 服务，配置 CORS 跨域。
- [ ] **Task 2.2: Mock API 开发**
    - 定义统一的 API 返回格式 `{"code": 200, "data": {}, "msg": "success"}`。
    - 在后端内存中定义固定的单词数据。
    - 实现 `/api/words/today`（返回固定单词列表）。
    - 实现 `/api/words/review`（接收前端的点击结果，目前仅在控制台打印）。
- [ ] **Task 2.3: 前端对接 API**
    - 前端配置 Axios。
    - 移除前端的硬编码数组，改为从后端 `/api/words/today` 获取数据。

## 🚩 Milestone 3: 数据库集成与用户映射 (Miss)
**目标**：引入真实的持久化存储，处理多用户与真实的词库数据。

- [ ] **Task 3.1: 数据库建模**
    - 集成 GORM 与 SQLite。
    - 设计表结构：`User`（用户映射）、`Word`（词库表）、`Progress`（进度表）。
- [ ] **Task 3.2: 基础业务逻辑迁移至数据库**
    - 实现账号登录接口（Account -> user_id）。
    - 编写初始化脚本导入 JSON 词库到 SQLite。
    - 改造 `/api/words/today`，从数据库中读取真实的待复习单词。

## 🚩 Milestone 4: FSRS 记忆算法集成 (Miss)
**目标**：引入科学记忆算法，完成整个项目的核心业务。

- [ ] **Task 4.1: 算法接入与状态记录**
    - 引入 `go-fsrs` 等算法库。
    - 改造 `/api/words/review` 接口：接收前端提交的按钮评价，结合历史状态计算新的 `NextReview`，落盘到 `Progress` 表。
- [ ] **Task 4.2: 最终验收**
    - 验证复习逻辑是否符合间隔重复算法预期。
    - UI 细节打磨及移动端体验最终优化。
